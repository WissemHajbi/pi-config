# Reminders

- Set `EIDTOR` to `nvim` (probably meant `EDITOR`): `setx EIDTOR nvim`
- Set `VISUAL` to `nvim`: `setx VISUAL nvim`
- Set `GH_TOKEN` for GitHub PR creation: `setx GH_TOKEN "your_github_token"`
